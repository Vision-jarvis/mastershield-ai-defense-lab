# MasterShield AI - runtime dependencies
# Every entry below is imported by the codebase. scipy arrives transitively
# via scikit-learn; websockets is required by uvicorn to serve the live
# stream endpoint even though it is not imported directly.

fastapi>=0.110.0
uvicorn>=0.28.0
websockets>=12.0
pydantic>=2.6.0

numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
joblib>=1.3.0
networkx>=3.2.0

python-docx>=1.1.0
pytest>=8.0.0
